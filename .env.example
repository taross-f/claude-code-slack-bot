# ──────────────────────────────────────────────────────────────
# Claude Code Slack Bot — environment variable reference
# Copy this file to .env and fill in your real values.
# NEVER commit .env to version control.
# ──────────────────────────────────────────────────────────────

# ── Slack App ─────────────────────────────────────────────────
# Bot User OAuth Token  (Settings > OAuth & Permissions)
SLACK_BOT_TOKEN=xoxb-...

# App-Level Token with connections:write scope  (Settings > Basic Information)
SLACK_APP_TOKEN=xapp-...

# Signing Secret  (Settings > Basic Information)
SLACK_SIGNING_SECRET=...

# ── Anthropic ─────────────────────────────────────────────────
# Required unless using Bedrock or Vertex (see below)
ANTHROPIC_API_KEY=sk-ant-...

# ── Claude behaviour ──────────────────────────────────────────
# Maximum cost (USD) allowed per query before Claude stops
CLAUDE_MAX_BUDGET_USD=1.0

# Maximum agentic turns per query
CLAUDE_MAX_TURNS=50

# ── Working directory ─────────────────────────────────────────
# Optional base directory.  When set, users can refer to
# sub-directories by short name instead of an absolute path.
# e.g. BASE_DIRECTORY=/workspace  ->  "cwd my-project" resolves
# to /workspace/my-project
BASE_DIRECTORY=/workspace

# ── SQLite database ───────────────────────────────────────────
# Absolute path inside the container.  Mount a volume here to
# persist sessions and working-directory settings across restarts.
DB_PATH=/data/bot.db

# ── Debug / logging ───────────────────────────────────────────
DEBUG=false

# ── Alternative AI providers (mutually exclusive with API key) ─
# Set to "1" to route requests through AWS Bedrock
CLAUDE_CODE_USE_BEDROCK=

# Set to "1" to route requests through Google Vertex AI
CLAUDE_CODE_USE_VERTEX=
